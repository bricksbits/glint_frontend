import 'package:flutter/material.dart';

class UploadMediaScreen extends StatelessWidget {
  const UploadMediaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Give Users options to upload the media'),
    );
  }
}
