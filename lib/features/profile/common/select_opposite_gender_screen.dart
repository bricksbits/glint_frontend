import 'package:flutter/material.dart';

class SelectOppositeGenderScreen extends StatelessWidget {
  const SelectOppositeGenderScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Who catches your eyes'),
    );
  }
}
