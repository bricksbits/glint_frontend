import 'package:flutter/material.dart';

class UpdateBioAndDetailsScreen extends StatelessWidget {
  const UpdateBioAndDetailsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(''
          'Update Bio and Details Screen'),
    );
  }
}
