import 'package:flutter/material.dart';

class PronounsScreen extends StatelessWidget {
  const PronounsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Enter your Pronouns and other interest screen'),
    );
  }
}
