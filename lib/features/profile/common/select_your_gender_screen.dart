import 'package:flutter/material.dart';

class SelectYourGenderScreen extends StatelessWidget {
  const SelectYourGenderScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Select your Gender Screen'),
    );
  }
}
