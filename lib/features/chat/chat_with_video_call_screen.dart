import 'package:flutter/material.dart';

class ChatWithVideoCallScreen extends StatelessWidget {
  const ChatWithVideoCallScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
          'Video Chat With specific ID screen,to be implemented in next release'),
    );
  }
}
