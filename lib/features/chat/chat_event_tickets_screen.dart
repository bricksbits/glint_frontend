import 'package:flutter/material.dart';

class ChatEventTicketsScreen extends StatelessWidget {
  const ChatEventTicketsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child:
          Text('Tickets distribution section, will redirect to Payment screen'),
    );
  }
}
