import 'package:flutter/material.dart';

class EventPeopleScreen extends StatelessWidget {
  const EventPeopleScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Event People screen specific to events tabs only'),
    );
  }
}
